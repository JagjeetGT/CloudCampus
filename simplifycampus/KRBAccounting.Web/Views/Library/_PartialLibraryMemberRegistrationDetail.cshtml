@model IEnumerable<KRBAccounting.Domain.Entities.ScLibraryMemberRegistration>
@if (Model.Count() > 0)
{
    foreach (var item in Model)
    {
    @Html.Partial("LibraryMemberRegistrationDetailEdit", item)
    }
}
