@model KRBAccounting.Domain.Entities.Currency
@{
    ViewBag.Title = "Log On";
}
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
@*@Html.CustomValidationSummary("Login was unsuccessful. Please correct the errors and try again.")*@
@using (Ajax.BeginForm("CurrencyEdit", "Master", new { controller = "Master" }, new AjaxOptions { HttpMethod = "Post", UpdateTargetId = "MsgError",OnSuccess = "successEdit"}, new { @class = "form-horizontal", id = "currency-form" }))
{
    <div id="MsgError"></div>
    @Html.HiddenFor(x=>x.Id)
     @Html.HiddenFor(x => x.CreatedById)
     @Html.HiddenFor(x => x.CreatedDate)
      <div style="width: 445px;">
      
       <div class="lag">
              <label class="label-90">
                  Code</label>
              <span class="field">
                  @Html.TextBoxFor(m => m.Code, new { @class = "input-80 input" })
                  @Html.ValidationMessageFor(m => m.Code)
                  <span class="loading Code">
                    <img title="w_loader_gr.gif" src="/Content/themes/img/loaders/w_loader_gr.gif" /></span>
              </span>
          </div>
          <div class="lag">
                  <label class="label-90">
                      Name</label>
                  <span class="field">
                      @Html.TextBoxFor(m => m.Name, new { @class = "input-295 input" })
                      @Html.ValidationMessageFor(m => m.Name)
                      <span class="loading Name">
                    <img title="w_loader_gr.gif" src="/Content/themes/img/loaders/w_loader_gr.gif" /></span>
                  </span>
              </div>
         
          <div class="lag">
              <label class="label-90">
                  Unit</label>
              <span class="field">
                  @Html.TextBoxFor(m => m.Unit, new { @class = "input-295 input" })
                  @Html.ValidationMessageFor(m => m.Unit)
              </span>
          </div>
      </div>
}
@*<script type="text/javascript">
    $("#Name").live("focusout", function () {
        var name = $(this).val();
        $.getJSON("/Master/IsCurrencyNameAvailable?name=" + name + "&id=@Model.Id", "", function (data, status, xhr) {
            if (data != '') {
                $("#MsgError").html(data);
                $("#Name").focus();
            }
        });
    });
    $("#Code").live("focusout", function () {
        var name = $(this).val();
        $.getJSON("/Master/IsCurrencyCodeAvailable?name=" + name + "&id=@Model.Id", "", function (data, status, xhr) {
            if (data != '') {
                $("#MsgError").html(data);
                $("#Code").focus();
            }
        });
    });
</script>*@