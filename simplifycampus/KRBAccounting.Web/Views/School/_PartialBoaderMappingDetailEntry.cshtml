@model IEnumerable<KRBAccounting.Domain.Entities.ScBoaderMapping>  
@foreach (var item in Model)
{
    @Html.Partial("_PartialBoaderMappingDetailEdit", item)
    
}
@if (Model.Count() > 0)
{
    @Html.Action("BoaderDetailAdd", new {sectionId = Model.FirstOrDefault().SectionId, classId = Model.FirstOrDefault().ClassId})
}
<script type="text/javascript">
    $(document).ready(function () {
        $('#table-examroutine-detail').tableDnD({
            onDrop: function (table, row) {
            },
            dragHandle: ".dragHandle"
        });

        $("#table-examroutine-detail tr").hover(function () {
            $(this.cells[0]).addClass('showDragHandle');
        }, function () {
            $(this.cells[0]).removeClass('showDragHandle');
        });
    });

</script>
